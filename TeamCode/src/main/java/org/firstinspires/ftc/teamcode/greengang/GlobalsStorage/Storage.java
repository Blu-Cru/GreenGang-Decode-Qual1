package org.firstinspires.ftc.teamcode.greengang.GlobalsStorage;

import com.pedropathing.geometry.Pose;

public class Storage {
    //Alliance
    public static boolean isRed = false;

    // Pose
    public static Pose lastPose = new Pose(18, 120, Math.toRadians(144));
    public static double autoAimFlywheelPow = 1575;
}